/**
 * The seven-page Navigation chrome was replaced by the shared JEA App Shell.
 * Feature teams should register slots through `@jea/app` instead of adding pages here.
 */
export type AppPage = 'workspace'

export function Navigation(): null {
  return null
}
