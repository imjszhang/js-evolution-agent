# JEA 0.2.0 认证清单

状态：**已放行**。日期：2026-08-22。[GitHub Release v0.2.0](https://github.com/imjszhang/js-evolution-agent/releases/tag/v0.2.0) 已发布，tag 指向已认证提交 `1536ca1873eaa521418acc092dd982aef2ef25b5`。本清单对应已关闭的 [#178](https://github.com/imjszhang/js-evolution-agent/issues/178)。

- 安全债：[#77](https://github.com/imjszhang/js-evolution-agent/issues/77)
- 平台：macOS Apple Silicon（`arm64`）only
- 签名：ad-hoc，未公证
- 官方认证：[run 32547147234](https://github.com/imjszhang/js-evolution-agent/actions/runs/32547147234)，build `0.2.0+1536ca1.20260822T024639`，`dirty=false`
- 资产挂载：[run 32550598473](https://github.com/imjszhang/js-evolution-agent/actions/runs/32550598473)，12 个 allowlisted 资产
- DMG SHA256：`849ae10ea7d63b45733209b24a1fa5f5d657203f2f7aca35f316eb9790e90fcf`
- ZIP SHA256：`37b1b5260fe9c46e1968df3ad83f0213a40704fa931241a06e6738d5ebae2bb2`

## 版本与产物

- [x] `npm run release:preflight -- --strict --json`：package、lockfile、资源、builder、产品身份与 ACP runtime 均为 `0.2.0`
- [x] `npm run desktop:package` 产出 `JEA-0.2.0-macos-arm64.dmg`、`JEA-0.2.0-macos-arm64.zip` 与 `SHA256SUMS`
- [x] stage 与最终产物扫描不含 `.env`、用户 runtime、开发者绝对路径或测试文件
- [x] `build-metadata.json` 记录 `0.2.0`、完整 commit SHA、时间、platform/arch，并且 `dirty=false`
- [x] package smoke、产品旅程、恢复矩阵、launch smoke、30 分钟 `soak-report.json` 与最终 `certification-evidence.json` 完整
- [x] 30 分钟 packaged soak 必须在 certification evidence 生成前通过；soak 与 evidence 必须来自同一 clean build id / commit

## 发布门禁

- [x] `npm run audit:ci` 保持在认证路径，#77 baseline 必须精确匹配且未过期
- [x] `jea audit closure --json` 通过 `policies/release/closure-target-0.2.0.json`；`closure-audit.json` 必须进入同 build 的 certification evidence
- [x] dirty source tree 继续被打包 provenance / publish guard 拒绝
- [x] publish guard 在缺失或不完整认证证据时 fail-closed
- [x] Attach Release Assets 只接受既有 `v0.2.0` Release 与 allowlist 内资产（含 `soak-report.json` 和 `certification-evidence.json`）
- [x] `run_id` 必须由 `gh api` 核实为 `.github/workflows/release-macos.yml` 的 successful run，且 `head_sha` 与目标 tag commit SHA 完全一致
- [x] 下载后再次核实 `build-metadata.json` 为 full commit + `dirty=false`，package smoke 已通过、认证状态为 `certified`，并在 attach 前运行 publish guard
- [x] 既有同名 Release asset 默认冲突失败；只有 dispatch 显式勾选 `replace_existing_assets` 才允许 `--clobber`
- [x] 操作者明确放行后才可创建 tag / Release；工作流不得自行发布

## 明确不做

- Intel macOS、Windows 或 Linux 安装包
- notarization、公证或自动更新
- 自动触发发布；认证与附件工作流继续只允许 manual dispatch / 显式调用
- 放宽 clean-tree guard、#77 baseline 或 publish fail-closed 规则
