# JEA 0.2.1 认证清单

状态：**已放行**。日期：2026-08-24。[GitHub Release v0.2.1](https://github.com/imjszhang/js-evolution-agent/releases/tag/v0.2.1) 已发布，tag 指向已认证提交 `1b58a39b4b54f213926ae6640ef77ad902deb457`。本清单对应已关闭的 [#202](https://github.com/imjszhang/js-evolution-agent/issues/202)。

- 安全债：[#77](https://github.com/imjszhang/js-evolution-agent/issues/77)
- 平台：macOS Apple Silicon（`arm64`）only
- 签名：ad-hoc，未公证
- 闭环门槛：继续使用冻结的 `policies/release/closure-target-0.2.0.json`（`0.2.0-belief-loop`）
- 官方认证：[run 32685878713](https://github.com/imjszhang/js-evolution-agent/actions/runs/32685878713)，build `0.2.1+1b58a39.20260824T031655`，`dirty=false`
- 资产挂载：[run 32688240101](https://github.com/imjszhang/js-evolution-agent/actions/runs/32688240101)，12 个 allowlisted 资产
- DMG SHA256：`9865dc6871206d0eca5325cb497a17ce471c92d470ec31d77a69bfae1ad10532`
- ZIP SHA256：`1e138f4450aaf1dc85a24e640e10c4a73206e470edb2f8828e690649cec29bbf`

## 纳入本补丁

- [#189](https://github.com/imjszhang/js-evolution-agent/pull/189) Desktop client lifecycle 自动管理 Cycle
- [#190](https://github.com/imjszhang/js-evolution-agent/pull/190) Cycle attach / catch-up 硬化
- [#196](https://github.com/imjszhang/js-evolution-agent/pull/196) 有界 claim 存储与 Desktop supervisor lease
- [#198](https://github.com/imjszhang/js-evolution-agent/pull/198) Rule 失败恢复有界
- [#199](https://github.com/imjszhang/js-evolution-agent/pull/199) Evidence journal inspect / rebuild / rotate / rollback
- [#186](https://github.com/imjszhang/js-evolution-agent/pull/186) Attach workflow 安装锁定依赖
- [#205](https://github.com/imjszhang/js-evolution-agent/pull/205) 认证路径允许 Channel coordinator 在同一 PID 上托管全部默认 role

## 版本与产物

- [x] `npm run release:preflight -- --strict --json`：package、lockfile、资源、builder、产品身份与 ACP runtime 均为 `0.2.1`
- [x] `npm run desktop:package` 产出 `JEA-0.2.1-macos-arm64.dmg`、`JEA-0.2.1-macos-arm64.zip` 与 `SHA256SUMS`
- [x] stage 与最终产物扫描不含 `.env`、用户 runtime、开发者绝对路径或测试文件
- [x] `build-metadata.json` 记录 `0.2.1`、完整 commit SHA、时间、platform/arch，并且 `dirty=false`
- [x] package smoke、产品旅程、恢复矩阵、launch smoke、30 分钟 `soak-report.json` 与最终 `certification-evidence.json` 完整
- [x] 30 分钟 packaged soak 必须在 certification evidence 生成前通过；soak 与 evidence 必须来自同一 clean build id / commit

## 发布门禁

- [x] `npm run audit:ci` 保持在认证路径，#77 baseline 必须精确匹配且未过期
- [x] `jea audit closure --json` 通过 `policies/release/closure-target-0.2.0.json`；`closure-audit.json` 必须进入同 build 的 certification evidence
- [x] dirty source tree 继续被打包 provenance / publish guard 拒绝
- [x] publish guard 在缺失或不完整认证证据时 fail-closed
- [x] Attach Release Assets 只接受既有 `v0.2.1` Release 与 allowlist 内资产（含 `soak-report.json` 和 `certification-evidence.json`）
- [x] `run_id` 必须由 `gh api` 核实为 `.github/workflows/release-macos.yml` 的 successful run，且 `head_sha` 与目标 tag commit SHA 完全一致
- [x] 下载后再次核实 `build-metadata.json` 为 full commit + `dirty=false`，package smoke 已通过、认证状态为 `certified`，并在 attach 前运行 publish guard
- [x] 既有同名 Release asset 默认冲突失败；只有 dispatch 显式勾选 `replace_existing_assets` 才允许 `--clobber`
- [x] 操作者明确放行后才可创建 tag / Release；工作流不得自行发布

## 明确不做

- Intel macOS、Windows 或 Linux 安装包
- notarization、公证或自动更新
- 自动触发发布；认证与附件工作流继续只允许 manual dispatch / 显式调用
- 放宽 clean-tree guard、#77 baseline 或 publish fail-closed 规则
- 改写冻结的 `0.2.0-belief-loop` closure target
- 纳入 #183、Dependabot 更新或 #201
