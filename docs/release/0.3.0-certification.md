# JEA 0.3.0 认证清单

状态：**wiring / in certification**（未放行）。日期：2026-08-26。本清单对应 [#216](https://github.com/imjszhang/js-evolution-agent/issues/216)，父 issue [#208](https://github.com/imjszhang/js-evolution-agent/issues/208)。**不要**把本文件读成已发布 GitHub Release；`v0.3.0` tag 与 Release 仍是操作者门禁。

已发布产品仍是 [v0.2.1](https://github.com/imjszhang/js-evolution-agent/releases/tag/v0.2.1)。本补丁只做认证接线、版本面与证据钩子。

- 安全债：[#77](https://github.com/imjszhang/js-evolution-agent/issues/77)
- 平台：macOS Apple Silicon（`arm64`）only
- 签名：ad-hoc，未公证
- 闭环门槛：继续使用冻结的 `policies/release/closure-target-0.2.0.json`（`0.2.0-belief-loop`），**字节级不变**
- 控制面门槛：新增 `policies/release/control-plane-target-0.3.0.json`（`0.3.0-reactor-control-plane`）
- 官方认证 / soak / attach：操作者剩余（见下方未勾选项）

## 纳入本补丁（已在 main 落地的运行时 / 产品）

- [#218](https://github.com/imjszhang/js-evolution-agent/issues/218) Reactor backlog baseline
- [#219](https://github.com/imjszhang/js-evolution-agent/issues/219) control-plane contracts
- [#220](https://github.com/imjszhang/js-evolution-agent/issues/220) shared LLM budget
- [#226](https://github.com/imjszhang/js-evolution-agent/pull/226) unified control-plane runtime（router / migration / scheduler / cognitive / projection）
- [#227](https://github.com/imjszhang/js-evolution-agent/pull/227) product surface

Issues #209–#215、#217、#201 已关闭。本 issue 不实现新的 Reactor 功能。

## 版本与产物

- [x] `npm run release:preflight -- --strict --json`：package、lockfile、资源、builder、产品身份与 ACP runtime 均为 `0.3.0`
- [x] Protocol `jea.client` 保持 `1.0.0`（additive）；控制面 *contract* 版本保持 `0.3.0`，不另造第二套 contract version
- [ ] `npm run desktop:package` 产出 `JEA-0.3.0-macos-arm64.dmg`、`JEA-0.3.0-macos-arm64.zip` 与 `SHA256SUMS`（macOS 操作者剩余）
- [x] stage 与最终产物扫描不含 `.env`、用户 runtime、开发者绝对路径或测试文件（Linux 接线 / 既有 scan 测试）
- [ ] `build-metadata.json` 记录 `0.3.0`、完整 commit SHA、时间、platform/arch，并且 `dirty=false`（官方 macOS 认证证据，操作者剩余）
- [x] Linux 有界 `release:recovery-matrix --bounded` 与 Client API Electron/Web conformance 保持可跑
- [ ] 30 分钟 packaged soak 与最终 `certification-evidence.json` `status=certified`（操作者剩余）
- [ ] 30 分钟 packaged soak 必须在 certification evidence 生成前通过；soak 与 evidence 必须来自同一 clean build id / commit

## 控制面验收（Linux / temp JEA_HOME / `--mock`）

- [x] 新目标 `policies/release/control-plane-target-0.3.0.json` 由 `jea audit control-plane --json` / `npm run audit:control-plane` 消费
- [x] Activation identity `(reactor, evidence_key, activation_policy_version)` 跨 journal generation 存活；`evaluateJournalGenerationChange.creates_work === false`
- [x] Ledger lanes/states/transitions；`handled` 终态；deferred/blocked 不能 claim
- [x] 合成 0.2.x → 0.3.0 rebuild/rollback：`handled_coverage=preserved`，`covered_index_only_lost=0`，不删权威 evidence
- [x] Scheduler：realtime 先于 replay；heartbeat / `worker_alive` 不蕴含 running/catching_up；预算耗尽 / `cycle_admission=parked` park-once
- [x] Scheduler crash/restart + `reclaim_lease_expired` 不把身份标成 handled
- [x] Replay 期间到达的 realtime 在下一 turn 被选中
- [x] Replay batch/wall/token/spend 边界
- [x] Cognitive：`no_decision_relevant_delta` 不调 LLM
- [x] Projection：last-good + freshness；Cognitive/Rule/Memory 不可相加；禁止 payload hydrate
- [x] Product mapping：heartbeat + 大量 replay ready 映射为 `listening` / `queued`，不是 `catching_up`
- [x] Pause/resume `evolution.state`
- [x] 干净新 subject：隔离源根 + `subject init` + `data init --all` + `run --mock`（cycle-e2e certification mock 等价路径；官方 macOS 工作流的默认 mock + closure 仍是操作者剩余）
- [x] 冻结 0.2.0 closure target 文件 SHA-256 不变；belief-binding / expected-output / causal / duplicate-settlement 在干净 mock run 后仍为 0。`evaluateClosureTarget` 可能因 `standing_memory.updated_at` 秒级截断把 `memory_freshness` 标成 `stale`（cursor 已是 `current`、嵌套 freshness 为 `fresh`）。认证接线记录该 gap，**不**改 closure-audit / Memory Reactor。
- [x] 预算恢复走既有 `jea llm budget` / `inspectLlmBudget` 共用账本，不另造第二套 Activation Ledger 或预算系统
- [x] `npm run perf:reactor-backlog-baseline -- --size tiny`（既有 vitest，不在本 PR 再跑第二遍 tiny）断言 `handled_coverage=preserved`
- [ ] packaged Desktop launch / catch-up / pause / quit / restart（Cloud 是 Linux，操作者剩余）

## 发布门禁

- [x] `npm run audit:ci` 保持在认证路径，#77 baseline 必须精确匹配且未过期
- [x] `jea audit closure --json` 继续对照冻结的 `policies/release/closure-target-0.2.0.json`；`closure-audit.json` 仍须进入同 build 的 certification evidence
- [x] `control-plane-audit.json` 已加入 allowlist 与 certification evidence 钩子；`ok === true` 且 `gate.target_id === '0.3.0-reactor-control-plane'`
- [x] dirty source tree 继续被打包 provenance / publish guard 拒绝
- [x] publish guard 在缺失或不完整认证证据时 fail-closed
- [ ] Attach Release Assets 只接受既有 `v0.3.0` Release 与 allowlist 内资产（含 `soak-report.json`、`closure-audit.json`、`control-plane-audit.json` 和 `certification-evidence.json`）
- [ ] `run_id` 必须由 `gh api` 核实为 `.github/workflows/release-macos.yml` 的 successful run，且 `head_sha` 与目标 tag commit SHA 完全一致
- [ ] 下载后再次核实 `build-metadata.json` 为 full commit + `dirty=false`，package smoke 已通过、认证状态为 `certified`，并在 attach 前运行 publish guard
- [x] 既有同名 Release asset 默认冲突失败；只有 dispatch 显式勾选 `replace_existing_assets` 才允许 `--clobber`
- [x] 操作者明确放行后才可创建 tag / Release；工作流不得自行发布
- [x] `incident` baseline 与 30 分钟 soak **不**进入 PR required CI

## 明确不做

- Intel macOS、Windows 或 Linux 安装包
- notarization、公证或自动更新
- 自动触发发布；认证与附件工作流继续只允许 manual dispatch / 显式调用
- 放宽 clean-tree guard、#77 baseline 或 publish fail-closed 规则
- 改写冻结的 `0.2.0-belief-loop` closure target
- 为了把认证变绿而改 scheduler / router / store / UI
- 另造第二套 Activation Ledger 或预算系统
- 在本 PR 创建 `v0.3.0` tag 或 GitHub Release
