# JEA 0.3.1 认证清单

状态：**已放行**。日期：2026-08-31。[GitHub Release v0.3.1](https://github.com/imjszhang/js-evolution-agent/releases/tag/v0.3.1) 已发布，tag 指向已认证提交 `61c599b660fdefcdd922b8acae07ad46abd7ffad`。本清单对应已关闭的 [#246](https://github.com/imjszhang/js-evolution-agent/issues/246)。

- 安全债：[#77](https://github.com/imjszhang/js-evolution-agent/issues/77)
- 平台：macOS Apple Silicon（`arm64`）only
- 签名：ad-hoc，未公证
- 闭环门槛：继续使用冻结的 `policies/release/closure-target-0.2.0.json`（`0.2.0-belief-loop`）
- 控制面门槛：继续使用冻结的 `policies/release/control-plane-target-0.3.0.json`（`0.3.0-reactor-control-plane`）
- 官方认证：[run 33377736946](https://github.com/imjszhang/js-evolution-agent/actions/runs/33377736946)，build `0.3.1+61c599b.20260831T092751`，`dirty=false`，`status=certified`
- 资产挂载：[run 33380470585](https://github.com/imjszhang/js-evolution-agent/actions/runs/33380470585)，13 个 allowlisted 资产
- DMG SHA256：`f324d1597417199c518ab56f4a71377e5818aefb891ab055921025d73ac10cb2`
- ZIP SHA256：`3729544b43d42893e9c33bc77a4d52e93f1c80d56f2b9579d1d76ae7bb29d417`

## 纳入本补丁

- [#233](https://github.com/imjszhang/js-evolution-agent/pull/233) 控制面生产接线：产品工作只跟 Activation Ledger
- [#241](https://github.com/imjszhang/js-evolution-agent/pull/241) Ledger 崩溃安全写入与 mixed-fixture 认证
- [#242](https://github.com/imjszhang/js-evolution-agent/pull/242) Ledger v2：有界 hot open work 与 terminal shard
- [#243](https://github.com/imjszhang/js-evolution-agent/pull/243) 产品可见的升级 / 迁移状态机
- [#240](https://github.com/imjszhang/js-evolution-agent/pull/240) 打包 Desktop lifecycle soak
- [#193](https://github.com/imjszhang/js-evolution-agent/pull/193) lucide-react 1.34.0
- [#234](https://github.com/imjszhang/js-evolution-agent/pull/234) 开发依赖 minor/patch
- [#195](https://github.com/imjszhang/js-evolution-agent/pull/195) `actions/download-artifact` 7 → 8
- [#194](https://github.com/imjszhang/js-evolution-agent/pull/194) CodeQL Action patch

## 版本与产物

- [x] `npm run release:preflight -- --strict --json`：package、lockfile、资源、builder、产品身份与 ACP runtime 均为 `0.3.1`
- [x] Protocol `jea.client` 保持 `1.0.0`（additive）；控制面 *contract* 版本保持 `0.3.0`，不另造第二套 contract version
- [x] `npm run desktop:package` 产出 `JEA-0.3.1-macos-arm64.dmg`、`JEA-0.3.1-macos-arm64.zip` 与 `SHA256SUMS`
- [x] stage 与最终产物扫描不含 `.env`、用户 runtime、开发者绝对路径或测试文件
- [x] `build-metadata.json` 记录 `0.3.1`、完整 commit SHA、时间、platform/arch，并且 `dirty=false`
- [x] package smoke、产品旅程、恢复矩阵、launch smoke、30 分钟 `soak-report.json` 与最终 `certification-evidence.json` 完整
- [x] 30 分钟 packaged soak 必须在 certification evidence 生成前通过；soak 与 evidence 必须来自同一 clean build id / commit

## 发布门禁

- [x] `npm run audit:ci` 保持在认证路径，#77 baseline 必须精确匹配且未过期
- [x] `jea audit closure --json` 通过 `policies/release/closure-target-0.2.0.json`；`closure-audit.json` 必须进入同 build 的 certification evidence
- [x] `control-plane-audit.json` 仍须进入同 build 的 certification evidence；`ok === true` 且 `gate.target_id === '0.3.0-reactor-control-plane'`
- [x] dirty source tree 继续被打包 provenance / publish guard 拒绝
- [x] publish guard 在缺失或不完整认证证据时 fail-closed
- [x] Attach Release Assets 只接受既有 `v0.3.1` Release 与 allowlist 内资产（含 `soak-report.json`、`closure-audit.json`、`control-plane-audit.json` 和 `certification-evidence.json`）
- [x] `run_id` 必须由 `gh api` 核实为 `.github/workflows/release-macos.yml` 的 successful run，且 `head_sha` 与目标 tag commit SHA 完全一致
- [x] 下载后再次核实 `build-metadata.json` 为 full commit + `dirty=false`，package smoke 已通过、认证状态为 `certified`，并在 attach 前运行 publish guard
- [x] 既有同名 Release asset 默认冲突失败；只有 dispatch 显式勾选 `replace_existing_assets` 才允许 `--clobber`
- [x] 操作者明确放行后才可创建 tag / Release；工作流不得自行发布

## 明确不做

- Intel macOS、Windows 或 Linux 安装包
- notarization、公证或自动更新
- 自动触发发布；认证与附件工作流继续只允许 manual dispatch / 显式调用
- 放宽 clean-tree guard、#77 baseline 或 publish fail-closed 规则
- 改写冻结的 `0.2.0-belief-loop` closure target
- 改写冻结的 `0.3.0-reactor-control-plane` 目标，或另造 0.3.1 control-plane contract
- 纳入 Dependabot 生产依赖组（#235 / #244）或未进 main 的 web-host stale-bundle 修复
